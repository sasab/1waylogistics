import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "1Way Global Logistics - Professional Freight & Shipping Services",
  description: "Leading logistics company offering express courier, air freight, ocean freight, warehousing, and customs clearance services worldwide. Fast, reliable, and cost-effective shipping solutions.",
  keywords: "logistics, freight forwarding, shipping, courier, air freight, ocean freight, warehousing, customs clearance, international shipping",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
